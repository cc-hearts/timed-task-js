# config

in the current folder create `app.yaml` files

```yaml
# mysql
mysql:
  host:
  user:
  port:
  password:
  database:

# redis
redis:
  port:
  host:
  db:
  password:

# puppeteerConfig
puppeteerConfig:
  port: 3001
  xnLogin:
    url:
    userName:
    password:
```
